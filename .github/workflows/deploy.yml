name: Deploy to Server

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build project
        run: npm run build
      
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SERVER_SSH_KEY }}
      
      - name: Deploy to server
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          SERVER_PATH: ${{ secrets.SERVER_PATH }}
        run: |
          # Создаем директорию на сервере, если её нет
          ssh -o StrictHostKeyChecking=no ${SERVER_USER}@${SERVER_HOST} "mkdir -p ${SERVER_PATH}"
          
          # Полностью очищаем папку на сервере (удаляем все старые файлы и папки)
          echo "Cleaning old files on server..."
          ssh -o StrictHostKeyChecking=no ${SERVER_USER}@${SERVER_HOST} "
            cd ${SERVER_PATH} && 
            rm -rf * .[^.]* 2>/dev/null || true &&
            find . -mindepth 1 -delete 2>/dev/null || true
          "
          
          # Копируем конфигурации nginx для API прокси
          echo "Copying nginx API proxy configs..."
          scp -o StrictHostKeyChecking=no nginx-api-proxy.conf ${SERVER_USER}@${SERVER_HOST}:/tmp/nginx-api-proxy.conf
          scp -o StrictHostKeyChecking=no nginx-calltouch-proxy.conf ${SERVER_USER}@${SERVER_HOST}:/tmp/nginx-calltouch-proxy.conf
          
          # Создаем директорию api на сервере перед копированием PHP файла
          echo "Creating /api directory on server..."
          ssh -o StrictHostKeyChecking=no ${SERVER_USER}@${SERVER_HOST} "mkdir -p ${SERVER_PATH}/api"
          
          # Копируем PHP прокси файлы на сервер
          echo "Copying PHP proxy files..."
          scp -o StrictHostKeyChecking=no api/lead-proxy.php ${SERVER_USER}@${SERVER_HOST}:${SERVER_PATH}/api/lead-proxy.php
          scp -o StrictHostKeyChecking=no api/calltouch-proxy.php ${SERVER_USER}@${SERVER_HOST}:${SERVER_PATH}/api/calltouch-proxy.php
          
          # Проверяем, что файлы скопировались и устанавливаем права
          echo "Verifying PHP files and setting permissions..."
          ssh -o StrictHostKeyChecking=no ${SERVER_USER}@${SERVER_HOST} "
            if [ -f ${SERVER_PATH}/api/lead-proxy.php ] && [ -f ${SERVER_PATH}/api/calltouch-proxy.php ]; then
              chmod 644 ${SERVER_PATH}/api/lead-proxy.php
              chmod 644 ${SERVER_PATH}/api/calltouch-proxy.php
              echo '✅ PHP файлы успешно скопированы'
              ls -la ${SERVER_PATH}/api/*.php
            else
              echo '❌ ОШИБКА: PHP файлы не найдены после копирования!'
              exit 1
            fi
          "
          
          # Копируем все собранные файлы из dist на сервер
          echo "Copying new files to server..."
          scp -o StrictHostKeyChecking=no -r dist/* ${SERVER_USER}@${SERVER_HOST}:${SERVER_PATH}/
          
          # Если нужно скопировать скрытые файлы (например, .htaccess), раскомментируйте:
          # scp -o StrictHostKeyChecking=no -r dist/.[^.]* ${SERVER_USER}@${SERVER_HOST}:${SERVER_PATH}/ 2>/dev/null || true
          
          # Настраиваем nginx для проксирования /api/lead-proxy
          echo "Configuring nginx for API proxy..."
          ssh -o StrictHostKeyChecking=no ${SERVER_USER}@${SERVER_HOST} "
            # Определяем версию PHP и путь к сокету
            PHP_SOCKET=\$(ls /var/run/php/php*-fpm.sock 2>/dev/null | head -1)
            if [ -z \"\$PHP_SOCKET\" ]; then
              # Пробуем через TCP
              PHP_SOCKET='127.0.0.1:9000'
              echo '⚠️  PHP-FPM socket не найден, используем TCP: '\$PHP_SOCKET
            else
              echo '✅ Найден PHP-FPM socket: '\$PHP_SOCKET
            fi
            
            # Определяем document_root из конфига nginx
            DOCUMENT_ROOT=\$(grep -E '^\s*root\s+' /etc/nginx/sites-enabled/*.conf 2>/dev/null | head -1 | awk '{print \$2}' | tr -d ';' || echo '${SERVER_PATH}')
            if [ -z \"\$DOCUMENT_ROOT\" ] || [ \"\$DOCUMENT_ROOT\" = \"${SERVER_PATH}\" ]; then
              DOCUMENT_ROOT='${SERVER_PATH}'
            fi
            echo '📁 Document root: '\$DOCUMENT_ROOT
            
            # Создаем конфигурацию с правильными путями
            sudo mkdir -p /etc/nginx/includes
            sudo sed \"s|DOCUMENT_ROOT_PLACEHOLDER|\$DOCUMENT_ROOT|g; s|PHP_SOCKET_PLACEHOLDER|\$PHP_SOCKET|g; s|root /var/www/fcriverclub.ru;|root \$DOCUMENT_ROOT;|g; s|fastcgi_pass unix:/var/run/php/php8.1-fpm.sock;|fastcgi_pass \$PHP_SOCKET;|g\" /tmp/nginx-api-proxy.conf > /tmp/api-proxy-custom.conf
            sudo cp /tmp/api-proxy-custom.conf /etc/nginx/includes/api-proxy.conf
            echo '✅ Конфигурация создана с правильными путями'
            echo '📋 Проверка конфигурации calltouch-proxy:'
            sudo grep -A 10 'location.*calltouch-proxy' /etc/nginx/includes/api-proxy.conf || echo '⚠️  calltouch-proxy location не найден'
            
            # Ищем активный конфиг nginx
            NGINX_CONF=\$(ls /etc/nginx/sites-enabled/*.conf 2>/dev/null | head -1)
            if [ -z \"\$NGINX_CONF\" ]; then
              NGINX_CONF=\"/etc/nginx/sites-available/default\"
            fi
            
            # Проверяем и обновляем конфигурацию nginx
            if ! grep -q 'include.*api-proxy.conf' \"\$NGINX_CONF\" 2>/dev/null; then
              # Добавляем include перед закрывающей скобкой server блока
              # Используем awk для более надежной вставки
              sudo awk '/^[[:space:]]*server[[:space:]]*{/,/^[[:space:]]*}/ {
                if (/^[[:space:]]*}/ && !added) {
                  print \"    include /etc/nginx/includes/api-proxy.conf;\"
                  added=1
                }
                print
              }' \"\$NGINX_CONF\" > \"\$NGINX_CONF.tmp\" && sudo mv \"\$NGINX_CONF.tmp\" \"\$NGINX_CONF\"
              echo 'ℹ️  Добавлен include api-proxy.conf в конфигурацию nginx'
            else
              echo 'ℹ️  Include api-proxy.conf уже присутствует в конфигурации'
            fi
            
            # Удаляем ВСЕ конфликтующие location блоки для /api/lead-proxy и /api/calltouch-proxy из основного конфига
            echo '🔍 Поиск и удаление конфликтующих location блоков...'
            # Удаляем location = /api/lead-proxy
            sudo sed -i '/location[[:space:]]*=[[:space:]]*\/api\/lead-proxy/,/^[[:space:]]*}/d' \"\$NGINX_CONF\" 2>/dev/null || true
            # Удаляем location /api/lead-proxy
            sudo sed -i '/location[[:space:]]*\/api\/lead-proxy/,/^[[:space:]]*}/d' \"\$NGINX_CONF\" 2>/dev/null || true
            # Удаляем location = /api/calltouch-proxy
            sudo sed -i '/location[[:space:]]*=[[:space:]]*\/api\/calltouch-proxy/,/^[[:space:]]*}/d' \"\$NGINX_CONF\" 2>/dev/null || true
            # Удаляем location /api/calltouch-proxy
            sudo sed -i '/location[[:space:]]*\/api\/calltouch-proxy/,/^[[:space:]]*}/d' \"\$NGINX_CONF\" 2>/dev/null || true
            echo '✅ Конфликтующие location блоки удалены (если были)'
            
            # calltouch-proxy уже включен в api-proxy.conf, отдельный файл не нужен
            echo '✅ calltouch-proxy включен в api-proxy.conf'
            
            # Проверяем еще раз после удаления
            if grep -q 'location.*\/api\/lead-proxy' \"\$NGINX_CONF\" 2>/dev/null; then
              echo '⚠️  Warning: Все еще есть location /api/lead-proxy в основном конфиге!'
              sudo grep -n 'location.*\/api\/lead-proxy' \"\$NGINX_CONF\" || true
            fi
            
            # Проверяем, что конфигурация правильно применена
            echo '🔍 Проверка nginx конфигурации:'
            if ! sudo nginx -t; then
              echo '❌ ОШИБКА: Конфигурация nginx невалидна!'
              exit 1
            fi
            
            # ПРИНУДИТЕЛЬНО перезагружаем nginx
            echo '🔄 ПРИНУДИТЕЛЬНАЯ перезагрузка nginx...'
            sudo systemctl restart nginx 2>/dev/null || sudo service nginx restart 2>/dev/null || {
              echo '⚠️  systemctl/service не работает, пробуем nginx -s reload...'
              sudo nginx -s reload
            }
            
            # Ждем секунду чтобы nginx перезапустился
            sleep 2
            
            # Проверяем, что nginx работает
            if sudo systemctl is-active --quiet nginx || sudo service nginx status > /dev/null 2>&1; then
              echo '✅ Nginx перезапущен и работает'
            else
              echo '⚠️ Warning: nginx может быть не перезапущен'
            fi
            
            # ФИНАЛЬНАЯ ПРОВЕРКА: проверяем что calltouch-proxy location есть в конфиге
            echo '🔍 ФИНАЛЬНАЯ ПРОВЕРКА конфигурации:'
            if sudo grep -q 'location.*calltouch-proxy' /etc/nginx/includes/api-proxy.conf 2>/dev/null; then
              echo '✅ calltouch-proxy location найден в api-proxy.conf'
              sudo grep -A 5 'location.*calltouch-proxy' /etc/nginx/includes/api-proxy.conf
            else
              echo '❌ ОШИБКА: calltouch-proxy location НЕ найден в api-proxy.conf!'
              exit 1
            fi
            
            # Проверяем что файл calltouch-proxy.php существует
            if [ -f \"\$DOCUMENT_ROOT/api/calltouch-proxy.php\" ]; then
              echo '✅ Файл calltouch-proxy.php существует'
              ls -la \"\$DOCUMENT_ROOT/api/calltouch-proxy.php\"
            else
              echo '❌ ОШИБКА: Файл calltouch-proxy.php НЕ найден!'
              exit 1
            fi
          " || echo "⚠️  Warning: nginx configuration step failed, but deployment continues"
          
          # Опционально: можно добавить команды для перезапуска сервиса
          # ssh -o StrictHostKeyChecking=no ${SERVER_USER}@${SERVER_HOST} "cd ${SERVER_PATH} && pm2 restart app || true"
          
          echo "✅ Deployment completed successfully! All old files were removed and replaced."
